var Dish = function (name, photo, description, ingredients) {
    this.name = name || "";
    this.photo = photo || "";
    this.description = description || "";
    this.ingredients = ingredients || [];
}